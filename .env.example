# Neon / Postgres — durable folder + file metadata (required for multi-device library)
# Create free DB: https://console.neon.tech → copy connection string
# Vercel: Project → Settings → Environment Variables → DATABASE_URL
DATABASE_URL=postgresql://user:password@ep-xxxx.region.aws.neon.tech/neondb?sslmode=require

# Required for real Shelby uploads — dedicated SERVICE wallet (not user Petra)
# Generate: node scripts/generate-service-wallet.mjs
# Fund testnet: https://aptos.dev/network/faucet
# Vercel → Environment Variables → APTOS_PRIVATE_KEY + APTOS_NETWORK=testnet → Redeploy
APTOS_PRIVATE_KEY=0xYOUR_TESTNET_SERVICE_WALLET_PRIVATE_KEY_HEX

# Network: testnet | shelbynet | local
APTOS_NETWORK=testnet

# Optional Shelby / Aptos API key
SHELBY_API_KEY=
# APTOS_API_KEY=

# Max encrypted payload bytes (default 10MB)
# MAX_UPLOAD_BYTES=10485760

# Browser (optional)
# VITE_APTOS_NETWORK=testnet
# VITE_SHELBY_API_KEY=
